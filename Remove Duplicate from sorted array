class Solution {
public:
    int removeDuplicates(vector<int>& nums) {
        map<int,int>mp;
        for(int i=0;i<nums.size();i++)
            mp[nums[i]]++;
        int k=0;
        for(auto it=mp.begin();it!=mp.end();it++)
        {
            nums[k++]=it->first;
        }
        return k;
    }
};
